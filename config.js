const token = '-token-censored-';

const colors = {
    success: 0x2dfc9f,
    error: 0xfc2d2d
}

const prefix = './';

const weeklyAmount = 30;

module.exports = { token, colors, prefix, weeklyAmount }